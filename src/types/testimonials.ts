export interface Testimonial {
  id: number;
  name: string;
  role: string;
  company: string;
  initials: string;
  reviewUrl: string;
}

export interface EmptyMessage {
  line1: string;
  line2: string;
}